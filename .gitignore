# Dependencies
node_modules/

# Build output
dist/

# Secrets — never commit real credentials. .env.example is the template and IS committed.
.env
.env.*
!.env.example

# Logs and OS noise
*.log
.DS_Store
Thumbs.db

# Personal study notes — never committed
interview-prep/
